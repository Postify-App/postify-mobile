import 'package:flutter/material.dart';

class InitiateBusinessWidget extends StatelessWidget {
  const InitiateBusinessWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}